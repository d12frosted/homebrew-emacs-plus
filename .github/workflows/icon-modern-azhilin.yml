# This file is automatically generated by iconset script.
#
# DO NOT MODIFY it manually.

name: Icon modern-azhilin

on:
  push:
    branches:
      - master
    paths:
      - "icons/modern-azhilin.icns"
      - ".github/workflows/icon-modern-azhilin.yml"
  pull_request:
    paths:
      - "icons/modern-azhilin.icns"
      - ".github/workflows/icon-modern-azhilin.yml"

jobs:
  build:
    runs-on: macos-12
    env:
      HOMEBREW_GITHUB_REF: ${{ github.head_ref || github.ref }}
      HOMEBREW_GITHUB_REPOSITORY: ${{ github.repository }}
      HOMEBREW_GITHUB_ACTOR: ${{ github.actor }}

    steps:
      - uses: actions/checkout@v4

      - name: Build emacs-plus with modern-azhilin icon
        run: brew install Aliases/$(readlink Aliases/emacs-plus) --with-modern-azhilin-icon

      - name: Test installation
        run: $(brew --prefix)/bin/emacs --batch --eval='(print (+ 2 2))'
