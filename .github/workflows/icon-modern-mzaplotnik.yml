# This file is automatically generated by iconset script.
#
# DO NOT MODIFY it manually.

name: Icon modern-mzaplotnik

on:
  push:
    branches:
      - master
    paths:
      - "icons/modern-mzaplotnik.icns"
      - ".github/workflows/icon-modern-mzaplotnik.yml"
  pull_request:
    paths:
      - "icons/modern-mzaplotnik.icns"
      - ".github/workflows/icon-modern-mzaplotnik.yml"

jobs:
  build:
    runs-on: macos-13
    env:
      HOMEBREW_GITHUB_REF: ${{ github.head_ref || github.ref }}
      HOMEBREW_GITHUB_REPOSITORY: ${{ github.repository }}
      HOMEBREW_GITHUB_ACTOR: ${{ github.actor }}

    steps:
      - uses: actions/checkout@v5

      - name: Build emacs-plus with modern-mzaplotnik icon
        run: brew install Aliases/$(readlink Aliases/emacs-plus) --with-modern-mzaplotnik-icon

      - name: Test installation
        run: $(brew --prefix)/bin/emacs --batch --eval='(print (+ 2 2))'
