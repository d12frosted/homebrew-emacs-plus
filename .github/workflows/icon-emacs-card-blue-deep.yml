# This file is automatically generated by iconset script.
#
# DO NOT MODIFY it manually.

name: Icon emacs-card-blue-deep

on:
  push:
    branches:
      - master
    paths:
      - "icons/emacs-card-blue-deep.icns"
      - ".github/workflows/icon-emacs-card-blue-deep.yml"
  pull_request:
    paths:
      - "icons/emacs-card-blue-deep.icns"
      - ".github/workflows/icon-emacs-card-blue-deep.yml"

jobs:
  build:
    runs-on: macos-13
    env:
      HOMEBREW_GITHUB_REF: ${{ github.head_ref || github.ref }}
      HOMEBREW_GITHUB_REPOSITORY: ${{ github.repository }}
      HOMEBREW_GITHUB_ACTOR: ${{ github.actor }}

    steps:
      - uses: actions/checkout@v4

      - name: Build emacs-plus with emacs-card-blue-deep icon
        run: brew install Aliases/$(readlink Aliases/emacs-plus) --with-emacs-card-blue-deep-icon

      - name: Test installation
        run: $(brew --prefix)/bin/emacs --batch --eval='(print (+ 2 2))'
