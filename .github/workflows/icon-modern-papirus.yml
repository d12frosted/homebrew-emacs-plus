# This file is automatically generated by iconset script.
#
# DO NOT MODIFY it manually.

name: Icon modern-papirus

on:
  push:
    branches:
      - master
    paths:
      - "icons/modern-papirus.icns"
      - ".github/workflows/icon-modern-papirus.yml"
  pull_request:
    paths:
      - "icons/modern-papirus.icns"
      - ".github/workflows/icon-modern-papirus.yml"

jobs:
  build:
    runs-on: macos-12
    env:
      HOMEBREW_GITHUB_REF: ${{ github.head_ref || github.ref }}
      HOMEBREW_GITHUB_REPOSITORY: ${{ github.repository }}
      HOMEBREW_GITHUB_ACTOR: ${{ github.actor }}

    steps:
      - uses: actions/checkout@v4

      - name: Build emacs-plus with modern-papirus icon
        run: brew install Aliases/$(readlink Aliases/emacs-plus) --with-modern-papirus-icon

      - name: Test installation
        run: $(brew --prefix)/bin/emacs --batch --eval='(print (+ 2 2))'
