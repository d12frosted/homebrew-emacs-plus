# This file is automatically generated by iconset script.
#
# DO NOT MODIFY it manually.

name: Icon EmacsIcon5

on:
  push:
    branches:
      - master
    paths:
      - "icons/EmacsIcon5.icns"
      - ".github/workflows/icon-EmacsIcon5.yml"
  pull_request:
    paths:
      - "icons/EmacsIcon5.icns"
      - ".github/workflows/icon-EmacsIcon5.yml"

jobs:
  build:
    runs-on: macos-13
    env:
      HOMEBREW_GITHUB_REF: ${{ github.head_ref || github.ref }}
      HOMEBREW_GITHUB_REPOSITORY: ${{ github.repository }}
      HOMEBREW_GITHUB_ACTOR: ${{ github.actor }}

    steps:
      - uses: actions/checkout@v5

      - name: Build emacs-plus with EmacsIcon5 icon
        run: brew install Aliases/$(readlink Aliases/emacs-plus) --with-EmacsIcon5-icon

      - name: Test installation
        run: $(brew --prefix)/bin/emacs --batch --eval='(print (+ 2 2))'
