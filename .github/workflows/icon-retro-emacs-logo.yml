# This file is automatically generated by iconset script.
#
# DO NOT MODIFY it manually.

name: Icon retro-emacs-logo

on:
  push:
    branches:
      - master
    paths:
      - "icons/retro-emacs-logo.icns"
      - ".github/workflows/icon-retro-emacs-logo.yml"
  pull_request:
    paths:
      - "icons/retro-emacs-logo.icns"
      - ".github/workflows/icon-retro-emacs-logo.yml"

jobs:
  build:
    runs-on: macos-13
    env:
      HOMEBREW_GITHUB_REF: ${{ github.head_ref || github.ref }}
      HOMEBREW_GITHUB_REPOSITORY: ${{ github.repository }}
      HOMEBREW_GITHUB_ACTOR: ${{ github.actor }}

    steps:
      - uses: actions/checkout@v4

      - name: Build emacs-plus with retro-emacs-logo icon
        run: brew install Aliases/$(readlink Aliases/emacs-plus) --with-retro-emacs-logo-icon

      - name: Test installation
        run: $(brew --prefix)/bin/emacs --batch --eval='(print (+ 2 2))'
