# This file is automatically generated by iconset script.
#
# DO NOT MODIFY it manually.

name: Icon nobu417-big-sur

on:
  push:
    branches:
      - master
    paths:
      - "icons/nobu417-big-sur.icns"
      - ".github/workflows/icon-nobu417-big-sur.yml"
  pull_request:
    paths:
      - "icons/nobu417-big-sur.icns"
      - ".github/workflows/icon-nobu417-big-sur.yml"

jobs:
  build:
    runs-on: macos-13
    env:
      HOMEBREW_GITHUB_REF: ${{ github.head_ref || github.ref }}
      HOMEBREW_GITHUB_REPOSITORY: ${{ github.repository }}
      HOMEBREW_GITHUB_ACTOR: ${{ github.actor }}

    steps:
      - uses: actions/checkout@v4

      - name: Build emacs-plus with nobu417-big-sur icon
        run: brew install Aliases/$(readlink Aliases/emacs-plus) --with-nobu417-big-sur-icon

      - name: Test installation
        run: $(brew --prefix)/bin/emacs --batch --eval='(print (+ 2 2))'
