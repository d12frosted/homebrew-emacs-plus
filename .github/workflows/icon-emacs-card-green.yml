# This file is automatically generated by iconset script.
#
# DO NOT MODIFY it manually.

name: Icon emacs-card-green

on:
  push:
    branches:
      - master
    paths:
      - "icons/emacs-card-green.icns"
      - ".github/workflows/icon-emacs-card-green.yml"
  pull_request:
    paths:
      - "icons/emacs-card-green.icns"
      - ".github/workflows/icon-emacs-card-green.yml"

jobs:
  build:
    runs-on: macos-13
    env:
      HOMEBREW_GITHUB_REF: ${{ github.head_ref || github.ref }}
      HOMEBREW_GITHUB_REPOSITORY: ${{ github.repository }}
      HOMEBREW_GITHUB_ACTOR: ${{ github.actor }}

    steps:
      - uses: actions/checkout@v4

      - name: Build emacs-plus with emacs-card-green icon
        run: brew install Aliases/$(readlink Aliases/emacs-plus) --with-emacs-card-green-icon

      - name: Test installation
        run: $(brew --prefix)/bin/emacs --batch --eval='(print (+ 2 2))'
